# mag-polishing-pipeline
Improves MAG quality by recruiting additional related samples through sequence similarity search. Assembles an initial sample via miassembler (optional), assembly MAG via genomes-generation pipeline (GGP), then queries branchwater for matching samples.
